# Generated by oblodai-backend tools/sdkgen from openapi.json - do not edit.
"""The signing protocol of the contract (``x-oblodai-signing``): the header names by role, the
canonical strings and the limits. The runtime reads them from here, so a header the core renames
reaches the SDK by regeneration alone.

A signed request: ``hex(HMAC-SHA256(secret, canonical))`` where canonical is the parts of
:data:`REQUEST_CANONICAL_ORDER` joined by :data:`REQUEST_CANONICAL_SEPARATOR`. A webhook delivery:
the same over the parts of :data:`WEBHOOK_CANONICAL_ORDER` joined by
:data:`WEBHOOK_CANONICAL_SEPARATOR`.
"""

from __future__ import annotations

from typing import Tuple

#: The MAC every signature is: lowercase hex of HMAC-SHA256.
SIGNATURE_ALGORITHM = "HMAC-SHA256, lowercase hex"

# -- signed request ----------------------------------------------------------------------------

#: Request header of the role ``public_id``.
HEADER_PUBLIC_ID = "X-Public-Id"
#: Request header of the role ``signature``.
HEADER_SIGNATURE = "X-Signature"
#: Request header of the role ``timestamp``.
HEADER_TIMESTAMP = "X-Timestamp"
#: Request header of the role ``idempotency_key``.
HEADER_IDEMPOTENCY_KEY = "Idempotency-Key"

#: Parts of the canonical request string, in order. ``ts``: unix seconds; ``METHOD``: the
#: upper-case method; ``request_uri``: path + raw query; ``idempotency_key``: the idempotency key
#: (empty without one); ``body``: the body bytes.
REQUEST_CANONICAL_ORDER: Tuple[str, ...] = (
    "ts",
    "METHOD",
    "request_uri",
    "idempotency_key",
    "body",
)
REQUEST_CANONICAL_SEPARATOR = "\n"

# -- webhook delivery --------------------------------------------------------------------------

#: Webhook delivery header of the role ``timestamp``.
HEADER_WEBHOOK_TIMESTAMP = "X-Webhook-Timestamp"
#: Webhook delivery header of the role ``signature``.
HEADER_WEBHOOK_SIGNATURE = "X-Webhook-Signature"
#: Webhook delivery header of the role ``signature_prev``.
HEADER_WEBHOOK_SIGNATURE_PREV = "X-Webhook-Signature-Prev"
#: Webhook delivery header of the role ``event``.
HEADER_WEBHOOK_EVENT = "X-Webhook-Event"
#: Webhook delivery header of the role ``id``.
HEADER_WEBHOOK_ID = "X-Webhook-Id"
#: Webhook delivery header of the role ``event_id``.
HEADER_WEBHOOK_EVENT_ID = "X-Webhook-Event-Id"
#: Webhook delivery header of the role ``event_time``.
HEADER_WEBHOOK_EVENT_TIME = "X-Webhook-Event-Time"
#: Rehearsal header (role ``test``): ``"true"`` on a test delivery, absent from a live one.
HEADER_WEBHOOK_TEST = "X-Webhook-Test"

#: Parts of the signed webhook string, in order. ``ts``: unix seconds; ``payload``: the raw body.
WEBHOOK_CANONICAL_ORDER: Tuple[str, ...] = ("ts", "payload")
WEBHOOK_CANONICAL_SEPARATOR = "."

# -- limits ------------------------------------------------------------------------------------

#: Accepted clock skew, seconds: the core's window for a signed request and the default window
#: for verifying a webhook delivery.
SKEW_SECONDS = 300
#: Largest request body the core accepts, bytes.
MAX_BODY = 1048576
#: Longest :data:`HEADER_IDEMPOTENCY_KEY` value the core accepts, characters.
MAX_IDEMPOTENCY_KEY_LENGTH = 255
