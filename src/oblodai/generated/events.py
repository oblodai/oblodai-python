# Generated by oblodai-backend tools/sdkgen from openapi.json - do not edit.
"""Webhook events of the API: the kind a delivery body names in ``type`` (the ``const`` of that
field in the body's schema), the model the body parses to, and the event names of each kind.
"""

from __future__ import annotations

from typing import Dict, Tuple, Type, Union

from .models import ConversionWebhook, PaymentWebhook, PayoutWebhook, WalletWebhook

#: The model of any known delivery body.
WebhookModel = Union[ConversionWebhook, PaymentWebhook, PayoutWebhook, WalletWebhook]

#: The event kinds (``type`` of a delivery body) the contract declares, sorted.
KNOWN_EVENT_KINDS: Tuple[str, ...] = ("conversion", "payment", "payout", "wallet")

#: ``kind -> model`` of the delivery body.
WEBHOOK_MODELS: Dict[str, Type[WebhookModel]] = {
    "conversion": ConversionWebhook,
    "payment": PaymentWebhook,
    "payout": PayoutWebhook,
    "wallet": WalletWebhook,
}

#: ``event name (X-Webhook-Event) -> kind``.
WEBHOOK_EVENTS: Dict[str, str] = {
    "conversion.completed": "conversion",
    "conversion.refunded": "conversion",
    "invoice.cancelled": "payment",
    "invoice.confirm_check": "payment",
    "invoice.created": "payment",
    "invoice.expired": "payment",
    "invoice.paid": "payment",
    "invoice.paid_over": "payment",
    "invoice.select": "payment",
    "invoice.under_review": "payment",
    "invoice.wrong_amount": "payment",
    "payout.approved": "payout",
    "payout.awaiting_cosign": "payout",
    "payout.broadcasting": "payout",
    "payout.cancelled": "payout",
    "payout.confirmed": "payout",
    "payout.failed": "payout",
    "payout.pending": "payout",
    "payout.sent": "payout",
    "wallet.paid": "wallet",
}
