# Generated by oblodai-backend tools/sdkgen from openapi.json - do not edit.
"""Long-running operations of the API (``x-sdk-poll``): which create calls return a job and how to
follow it until its status is terminal.
"""

from __future__ import annotations

from typing import Dict, FrozenSet

# --- runtime imports: the only names taken from the hand-written runtime; adjust here ---
from ..core.route import Poll

#: ``create operationId -> poll operationId``.
LRO: Dict[str, str] = {
    "createTransferBatch": "getBatchInfo",
    "createPaymentBatch": "getBatchInfo",
    "createRefundBatch": "getBatchInfo",
    "createPayoutBatch": "getBatchInfo",
    "createDocumentJob": "getDocumentJob",
}

#: ``poll operationId -> how to follow it``.
POLLS: Dict[str, Poll] = {
    "getBatchInfo": Poll(
        id_field="batch_id",
        model="BatchInfoResponse",
        status_field="status",
        terminal=("completed", "stopped"),
        download=None,
    ),
    "getDocumentJob": Poll(
        id_field="job_id",
        model="DocumentJobView",
        status_field="status",
        terminal=("done", "failed", "expired"),
        download="downloadDocumentJobFile",
    ),
}

#: Every status after which some job no longer changes.
TERMINAL_STATUSES: FrozenSet[str] = frozenset({"completed", "done", "expired", "failed", "stopped"})
